{
  "name": "@treety/pi",
  "version": "0.0.0",
  "private": true,
  "description": "Connect Pi sessions to TreeTY terminal resume and attention state.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test dist/*.test.js"
  }
}
