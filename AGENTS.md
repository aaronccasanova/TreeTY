# Package versioning

Include the appropriate semantic version bump whenever a change affects a package's shipped behavior. For Pi package changes, bump the root `package.json` and `packages/pi/package.json` together and keep their versions identical. Update any documentation or changelog that states the affected package version. Follow `RELEASING.md` for the complete release workflow.
